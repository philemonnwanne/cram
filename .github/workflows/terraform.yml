name: Deploy AZURE app
run-name: Action performed by ${{ github.actor }} 👻
# on: [push]
on: push
env:
  mascot: Mona

jobs:
  linux_job:
    runs-on: ubuntu-latest
    env:
      mascot: Octocat
    steps:
      - run: echo 'Hi ${{ env.mascot }} 1' # Hi Octocat
      - run: echo 'Hi ${{ env.mascot }} 2' # Hi cartier
        env:
          mascot: cartier
      - name: Check nginx
        run: |
          sudo systemctl start nginx
          systemctl status nginx
          pwd && ls -l
          

  # linux_job_2:
  #   runs-on: ubuntu-latest
  #   env:
  #     mascot: Tux
  #   steps:
  #     - run: echo 'Hi ${{ env.mascot }} 1' # Hi Tux
  #     - run: echo 'Hi ${{ env.mascot }} 2' # Hi Tux
  #     - run: echo 'Hi ${{ env.mascot }} 3' # Hi Tux
